## 用本地配置
server:
  port: 8030 # 启动端口
  application:
    name: quinn-service

spring:
  profiles:
    active: dev
  datasource:
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

  thymeleaf:
    prefix: classpath:static/templates/
    suffix: .html
    cache: false
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html

management:
  endpoints:
    web:
      exposure:
        include: "*"

feign:
  hystrix:
    enabled: true

mybatis:
  config-location: classpath:config/mybatis.xml
  mapper-locations: classpath*:mapper/**/*.xml

logging:
  config: classpath:config/logback-${spring.profiles.active}.xml

com:
  quinn-service:
    redis:
      host: 212.64.54.213
      port: 6379
      password: ENC(d43wbnxOOkVFPit7L3Mje9N2PG1ljiin8hvXg7snYtXFOjTtvvZLPw==)
      database: 0
      maxActive: 8
      maxIdle: 8
      maxWait: -1
      minIdle: 0
      timeout: 2000
      cache:
        name: quinn_service
      keys:
        namespace: "quinn_service:"
        normalize: true

fdfs:
  so-timeout: 1501
  connect-timeout: 601
  thumb-image:
    width: 150
    height: 150
  tracker-list:
    - 212.64.54.213:22122
  pool:
    max-total: -1
    max-wait-millis: 5000
    max-total-per-key: 50
    max-idle-per-key: 10
    max_idle_per_key: 5

---
# 开发环境
spring:
  profiles: dev
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://212.64.54.213:3306/bpm7?useUnicode=true&characterEncoding=UTF8&serverTimezone=GMT%2B8
    username: ENC(lcEeIWfw+Z27nLRoQblA7BoQxdb/hCtcDOPQmlXyGNI=)
    password: ENC(NqdI8taMA3DDg/3J0eeJcRU6Bg73Jq1FIaU4HWL5rgeUPwlh0qZCVw==)
    filters: stat
    initialSize: 1
    maxActive: 20
    minIdle: 1
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    maxOpenPreparedStatements: 20

---
# 测试环境
spring:
  profiles: qas

---
# 生产环境
spring:
  profiles: prd
